image:
  repository: fedlify/cicd-example
  tag: latest
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  port: 80
  targetPort: 3000

ingress:
  enabled: true
  className: nginx
  host: app.fedlify.com
  path: /
  tls: true